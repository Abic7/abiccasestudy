# Workflows

The n8n workflow JSON, prompt code, and parse logic for this pipeline are not published as part of this submission.

The following are available in the repo root and `/docs`:

- Architecture documentation and design decisions — `docs/architecture.md`
- Full assumptions log — `docs/assumptions.md`
- Verified sample output for both accounts — `docs/sample-output-meridian.md`, `docs/sample-output-northfield.md`
- Animated pipeline diagram — `architecture.html`

These demonstrate the scope and quality of the build without exposing the implementation.

*All workflow IP remains with Abi C.*
